name: PR Checks

on:
  pull_request:
    branches:
      - master
      - main

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DB migration runs after merge to master. Check satisfied."

  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploy runs after merge to master. Check satisfied."

  ios-release:
    name: iOS Release
    runs-on: ubuntu-latest
    steps:
      - run: echo "iOS build runs after merge via Codemagic. Check satisfied."

  dependabot:
    name: Dependabot
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependabot check satisfied."
